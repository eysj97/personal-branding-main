# 이 파일을 .env로 복사하고 키를 채워 넣으세요. .env는 .gitignore에 있어 커밋되지 않습니다.
#
# 스냅킵 "스캔/넣기"의 AI 분석에 쓰입니다. 키는 Vite 개발 서버(Node) 안에서만
# 읽히고 브라우저 번들에는 들어가지 않습니다.
# 키가 없거나 npm run dev가 아닌 정적 빌드로 열면, 분석은 자동으로
# 로컬 휴리스틱(비율·밝기·채도 계산)으로 대체됩니다.
#
# 키 발급: https://platform.claude.com/settings/keys
ANTHROPIC_API_KEY=
